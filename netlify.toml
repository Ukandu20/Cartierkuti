[build]
  # If your front-end lives in `frontend/`
  base    = "frontend"
  command = "npm ci && npm run build"
  # Vite outputs into `dist/` by default
  publish = "dist"

# Single-page‐app routing
[[redirects]]
  from = "/*"
  to   = "/index.html"
  status = 200
